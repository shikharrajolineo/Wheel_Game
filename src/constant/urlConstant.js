import { server_url } from "./appConstant";

export const OtpUrl = `${server_url}/send-otp`;

export const verifyOtpUrl = `${server_url}/verify-otp`;

export const giftUrl = `${server_url}/send-gift`

export const categoryUrl = `${server_url}/customer`;

export const storeNameUrl = `${server_url}/store`;